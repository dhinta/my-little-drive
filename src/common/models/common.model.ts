export enum DateFormat {
  SHORT = 'short',
  LONG = 'long',
  FULL = 'full',
}

export enum DateTimeFormat {
  SHORT = 'short',
  LONG = 'long',
  FULL = 'full',
}

export enum DialogType {
  NONE = 'none',
  FOLDER = 'folder',
  NOTE = 'note',
  DOCUMENT = 'document',
}

export const ALT_C = 'ALT-C';
